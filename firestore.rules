
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to the entire database.
    // This is permissive and suitable for a public demo application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
