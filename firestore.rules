rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to the leaderboard for this demo app.
    // In a production app, you would want to restrict write access.
    match /leaderboard/{entry} {
      allow read, write: if true;
    }
  }
}
